from . constants import (
    DEFAULT_HOST, DEFAULT_PORT, DEFAULT_REALM, DEFAULT_ROLES)


class CrossBar(object):
    name = "Crossbar"
    host = DEFAULT_HOST
    port = DEFAULT_PORT
    realm = DEFAULT_REALM
    roles = DEFAULT_ROLES
